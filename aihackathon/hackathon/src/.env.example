# Copy this file to .env and fill in your values.
# Leave EXPO_PUBLIC_API_URL blank to run fully offline (AI fallback mode).

# Your backend URL (Next.js / Express)
# POST /api/journal/analyze  →  { mood, tags, content }  →  { insight, distortions }
EXPO_PUBLIC_API_URL=
